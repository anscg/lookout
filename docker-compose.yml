services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: collapse
      POSTGRES_USER: collapse
      POSTGRES_PASSWORD: collapse
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U collapse"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  pgdata:
